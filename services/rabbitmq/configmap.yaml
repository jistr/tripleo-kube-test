kind: ConfigMap
apiVersion: v1
metadata:
  name: rabbitmq-kolla-config
  namespace: default
  labels:
    service: rabbitmq
data:
  kolla-config: |
    {
      "command": "rabbitmq-server",
      "config_files": []
    }
